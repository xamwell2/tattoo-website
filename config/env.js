// config/env.js
require("dotenv").config(); // This loads the environment variables from .env file

// Optionally log environment variables to ensure they are loaded
console.log("Environment variables loaded");
